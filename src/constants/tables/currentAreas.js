const getCurrentAreasColumns = ( ) => ([
    {
      title: 'Nombre',
      dataIndex: 'name',
      key: 'name',
    },
  ]);

export {
  getCurrentAreasColumns,
};

export default getCurrentAreasColumns;